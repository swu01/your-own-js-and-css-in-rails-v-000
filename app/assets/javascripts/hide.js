function hideWhenClicked() {
  #hide_this = 
}

const to_hide = document.getElemetnById('hide_this')

to_hide.addEventListener('click', function hideWhenClicked(element) {
  documen.tgetElementById('hide_this').style.display = 'none';
});
